 function dienTich(){
    let inputWidth,area;
    let inputHeight;
    inputWidth = prompt("Enter the width");
    inputHeight = prompt("Enter the height");

    area = parseInt(inputWidth*inputHeight);
    alert(area);
 }
